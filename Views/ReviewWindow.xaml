<Window x:Class="GameSnapPlugin.Views.ReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource LOCGameSnapReview_Title}"
        Height="620" Width="960"
        MinHeight="480" MinWidth="720"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a2e">

    <Window.Resources>
        <Style x:Key="DarkListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
        </Style>
        <Style x:Key="DarkListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0f3460"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a4a7a"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#c73652"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#555"/>
                                <Setter Property="Foreground" Value="#999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1a4a7a"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="52"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0f3460" Padding="16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource LOCGameSnapReview_Header}"
                           Foreground="White" FontSize="15" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding StatusText}"
                           Foreground="#a0b4c8" FontSize="12"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Unmatched files list -->
            <Border Grid.Column="0" Background="#16213e" BorderBrush="#0f3460" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#0f3460" Padding="10,0">
                        <TextBlock Text="{DynamicResource LOCGameSnapReview_UnmatchedFiles}" Foreground="#a0b4c8"
                                   FontSize="11" FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding UnmatchedFiles}"
                             SelectedItem="{Binding SelectedFile}"
                             Style="{StaticResource DarkListBox}"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource DarkListBoxItem}"/>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding FileName}"
                                               TextWrapping="Wrap"
                                               FontSize="12"
                                               Foreground="#e0e0e0"/>
                                    <TextBlock Text="{Binding DateText}"
                                               FontSize="10"
                                               Foreground="#607080"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Center: Image preview -->
            <Grid Grid.Column="1" Background="#0d0d1a">
                <TextBlock Text="{DynamicResource LOCGameSnapReview_SelectPreview}"
                           Foreground="#404060"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="14"
                           Visibility="{Binding NoPreviewVisibility}"/>
                <Image Source="{Binding PreviewImage}"
                       Stretch="Uniform"
                       Margin="12"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <Border Background="#cc000000" VerticalAlignment="Bottom" Padding="12,6"
                        Visibility="{Binding PreviewInfoVisibility}">
                    <TextBlock Text="{Binding PreviewInfo}"
                               Foreground="#c0d0e0" FontSize="11"/>
                </Border>
            </Grid>

            <!-- Right: Game list -->
            <Border Grid.Column="2" Background="#16213e" BorderBrush="#0f3460" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="36"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#0f3460" Padding="10,0">
                        <TextBlock Text="{DynamicResource LOCGameSnapReview_AssignToGame}" Foreground="#a0b4c8"
                                   FontSize="11" FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <TextBox Grid.Row="1"
                             Text="{Binding GameFilter, UpdateSourceTrigger=PropertyChanged}"
                             Background="#1e2d40" Foreground="#e0e0e0"
                             BorderBrush="#0f3460" BorderThickness="0,0,0,1"
                             Padding="10,6" FontSize="12"
                             x:Name="SearchBox">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <Grid>
                                                    <TextBlock Text="{DynamicResource LOCGameSnapReview_SearchGames}"
                                                               Foreground="#405060" FontSize="12"
                                                               VerticalAlignment="Center"
                                                               Visibility="{Binding PlaceholderVisibility}"/>
                                                    <ScrollViewer x:Name="PART_ContentHost" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding FilteredGames}"
                             SelectedItem="{Binding SelectedGame}"
                             Style="{StaticResource DarkListBox}"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource DarkListBoxItem}"/>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"
                                           TextWrapping="Wrap"
                                           FontSize="12"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#0f3460" Padding="16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                    <Button Content="{DynamicResource LOCGameSnapReview_AssignButton}"
                            Style="{StaticResource ActionButton}"
                            Command="{Binding AssignCommand}"
                            Margin="0,0,8,0"/>
                    <Button Content="{DynamicResource LOCGameSnapReview_DeleteButton}"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding DeleteCommand}"
                            Margin="0,0,8,0"/>
                    <Button Content="{DynamicResource LOCGameSnapReview_SkipButton}"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding SkipCommand}"/>
                </StackPanel>

                <Button Grid.Column="1" Content="{DynamicResource LOCGameSnapReview_CloseButton}"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding CloseCommand}"/>
            </Grid>
        </Border>
    </Grid>
</Window>